import React from 'react'

const Default = () => {
  return (
    <div>
      <h3>In React, the export default keywords are used to export a single value (usually a component) from a module. This allows you to import that value directly into another module without having to use curly braces.</h3>
    </div>
  )
}

export default Default
